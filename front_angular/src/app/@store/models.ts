export interface BackDatas {
  currentId: number;
  // currentUrl: string;
  currentIaDisplay: string;
  nextId: number;
  // nextUrl: string;
  nextIaDisplay: string;
  questionInterferente: string;
}
